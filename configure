#!/bin/bash
set -e
scripts_dir="$( dirname "${BASH_SOURCE[0]}" )/Scripts"

# install cmake and dependencies
"$scripts_dir/InstallDeps.sh"

# generate cmake with clang as default
"$scripts_dir/GenerateClang.sh"
