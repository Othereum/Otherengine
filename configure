#!/bin/bash
set -e
work_dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

# sdl2 glew
sudo apt install -y libsdl2-dev libglew-dev

# generate cmake with clang as default
cd $work_dir
./GenerateClang.sh
